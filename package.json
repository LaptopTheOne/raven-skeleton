{
  "name": "raven-skeleton",
  "version": "1.0.0",
  "description": "11ty based skeleton which will import MF components TODO",
  "main": "index.js",
  "scripts": {
    "start": "npm run build && npx @11ty/eleventy --serve",
    "start-production": "npm run build-production && npx @11ty/eleventy --serve",
    "build-production": "echo \"export const RAVEN_IDENTITY_API='https://ancient-fog-7097.fly.dev';\" > ./js/env.js",
    "build": "echo \"export const RAVEN_IDENTITY_API='http://localhost:3000';\" > ./js/env.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/LaptopTheOne/raven-skeleton.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/LaptopTheOne/raven-skeleton/issues"
  },
  "homepage": "https://github.com/LaptopTheOne/raven-skeleton#readme",
  "devDependencies": {
    "@11ty/eleventy": "^2.0.0"
  },
  "dependencies": {
    "dotenv": "^16.0.3"
  }
}
